'use strict';

/**
 * @ngdoc function
 * @name quoteApp.controller:RegisterCtrl
 * @description
 * # RegisterCtrl
 * Controller of the quoteApp
 */
angular.module('quoteApp')
  .controller('RegisterCtrl', function ($scope) {
    $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
